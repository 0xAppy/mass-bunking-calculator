[project]
name = "mass-bunking-calculator"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "0xAppy", email = "arpitpotdar2@gmail.com" }
]
requires-python = ">=3.13"
dependencies = [
    "flask>=3.1.3",
    "flask-cors>=6.0.5",
    "pywebview>=6.2.1",
]

[project.scripts]
mass-bunking-calculator = "mass_bunking_calculator:main"

[build-system]
requires = ["uv_build>=0.12.8,<0.13.0"]
build-backend = "uv_build"
